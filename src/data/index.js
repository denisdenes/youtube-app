import { combineReducers } from 'redux';

export const videos = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default combineReducers({
  videos
});
